# Miscellaneous
*.class
*.log
*.pyc
*.swp
*.mp4
*.mov
*.MOV
_tmp_*
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# VS Code
.vscode/

# Flutter / Dart / Pub
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
**/generated_plugin_registrant.dart

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# Android
/android/app/debug
/android/app/profile
/android/app/release
android/.gradle/
android/captures/
android/local.properties
android/key.properties
**/*.keystore
**/*.jks
android/**/GeneratedPluginRegistrant.java

# iOS
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
ios/Flutter/flutter_export_environment.sh
ios/Flutter/Generated.xcconfig
ios/Flutter/ephemeral/
ios/Runner/GeneratedPluginRegistrant.*
ios/Runner.xcworkspace/xcuserdata/
ios/Runner.xcodeproj/xcuserdata/
ios/Runner.xcodeproj/project.xcworkspace/xcuserdata/

# Coverage
coverage/

# Codemagic local cache
.cm-cache/

# Card-pack source archives + scratch space. The 48-card collection
# is shipped via the compressed WebPs under `assets/cards/`; the source
# zips and extracted PNGs are preserved locally as backups but kept
# out of git so a repo clone doesn't pull ~130 MB of binaries that
# duplicate already-tracked content.
docs/Cards/_extracted/
docs/Cards/*.zip

# Book build artifacts (generated print PDFs + Python venv)
book/build/out/
book/build/.venv/
book/build/__pycache__/

# Screenshot pipeline build cache
screenshots/build/__pycache__/

# SECRETS — never commit
*.p8
*.p12
*.mobileprovision
*.keystore.json
*.pem
*.key
cert_key
cert_key.pub
.env
.env.local

# Marketing video outputs — regeneratable from tools/build_trailer_video.py.
# Trailers can run 15–22 MB each; treat them as build artifacts, not assets.
# Leading slash is intentional — matches ONLY the root-level marketing/ dir,
# not assets/audio/marketing/ which legitimately holds tracked VO MP3s.
/marketing/

# Book cover mockups — regeneratable from book/build/_render_title_mockup.py.
# These are design exploration renders, not source-of-truth.
book/mockups/

# Raw screen recordings — over GitHub's 100 MB push limit. Keep locally
# as the master input to the trailer pipeline; gitignore so they don't
# block pushes. Re-record into the same path when needed.
#
# Going forward, recordings should land in /recordings/ (cleaner than
# co-mingling with assets/images/ which gets bundled into the iOS app).
# The two paths below are kept for backward-compat — old recordings
# still living there continue to be ignored.
/recordings/
assets/images/ScreenRecording*.MP4
branding/ScreenRecording*.MP4

# Merch print-ready PNGs — ~100 MB of regeneratable artifacts produced
# by tools/generate_merch_designs.py. Same policy as book/build/out/
# (regen from source, don't bloat the repo). Run the script after a
# fresh clone if you need the files locally.
commerce/print_ready/

# Book 2 production — uncompressed masters (regen from source, ship the
# compressed_* variants only). interior.pdf at 598 MB would exceed
# GitHub's 100 MB hard limit. Same policy as book/build/out/.
book/build_book2/out/interior.pdf
book/build_book2/out/interior_spreads.pdf
book/build_book2/out/review_spreads.pdf
book/build_book2/out/cover_wrap.pdf

# CryptoChris8 neon b-roll — large iPhone .MOV, not pipeline-source.
# Re-record if needed.
IMG_*.MOV
