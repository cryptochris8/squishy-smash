# Decal Sprites — PLACEHOLDER

Wall splat sprites (256x256 PNG, alpha-trimmed).
Today the game uses procedural alpha circles from `decal_manager.dart`.
Swap to sprite-based decals by replacing the `_DecalSplat.render()` body
with `Sprite.render(canvas, ...)`.
